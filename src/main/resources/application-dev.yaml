spring:
  datasource:
    url: jdbc:mysql://${LOCAL_DB_URL}/${LOCAL_DB_NAME}?useSSL=true&requireSSL=true&serverTimezone=UTC
    username: ${LOCAL_DB_USERNAME}
    password: ${LOCAL_DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver